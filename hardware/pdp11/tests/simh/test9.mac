	.TITLE test9
	.ASECT
	.=500
	mov	#500, sp
	mov	#200, r1
	mov	#300, r2
	cmp	r1, r2
	cmp	(r1), (r1)
	cmp	(r1)+, (r1)+
	cmp	@(r1)+, @(r1)+
	clr	r1
	clr	(r1)
	clr	(r1)+
	clr	@(r1)+
	clr	-(r1)
	clr	@-(r1)
	halt
