начало
  ф32 a := случ();
  ф32 ИнтервалN;

  если a < 0.5 то печать(1) все;

  если a < 0.5 то печать(1)
  иначе печать(4) все;

  если a < 0.5 то 
    печать(1);
    ИнтервалN := 1
  инес a < 0.7 то
    печать(2);
    ИнтервалN := 2
  инес a < 0.9 то
    печать(3);
    ИнтервалN := 3
  иначе
    печать(4);
    ИнтервалN := 4
  все;

  выбор ИнтервалN из
  1: печать(1),
  2: печать(2),
  3: печать(3),
  4: печать(4)
  иначе
    печать(-1)
  всевыб
конец
