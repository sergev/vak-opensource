#CXX     = clang++ -std=c++20 -stdlib=libc++ -fcoroutines-ts
CXX     = g++ -std=c++20 -fcoroutines #-stdlib=libc++ -fcoroutines-ts
CXXFLAGS = -g -O1
PROG    = demo

all:    $(PROG)

clean:
	rm -f $(PROG)
